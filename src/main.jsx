import React from 'react';
import ReactDOM from 'react-dom/client';
import CIBCParser from './CIBCParser';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <CIBCParser />
  </React.StrictMode>
);
